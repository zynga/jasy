(function() {
	Module("jasy.EventManager", {
		addListener : function(target, type, callback, context, capture) {

		},

		removeListener : function(target, type, callback, context, capture) {

		},

		cleanupObject : function(target) {

		},

		fireEvent : function(eventObj) {
			var target = eventObj.getTarget();
			
			
		}
	});
})();
 