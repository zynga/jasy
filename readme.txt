New tool chain based on qooxdoo's build system.

Goals:
- Better modularity
- Separate callable applications: jscompile, jsapi, jslint, jslocalize, ...
- Each application is available as API as well to build custom python-based compile scripts (somehow like SCons)

